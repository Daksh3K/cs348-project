generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "cockroachdb"
  url      = env("DATABASE_URL")
}

model hello_world {
  message String @db.String(255)
  id      BigInt @id

}
